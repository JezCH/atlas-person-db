name: Phase 7B Observability and Rollback

on:
  pull_request:
    paths:
      - "atlas-reader-observability.js"
      - "atlas-reader.js"
      - "atlas-production-source.js"
      - "app.js"
      - "index.html"
      - "migration/phase-7/scripts/phase7b-contract.mjs"
      - "migration/phase-7/PHASE_7_ROLLBACK_RUNBOOK.md"
      - ".github/workflows/phase-7b-observability-rollback.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  contract:
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Use Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: Run Phase 7A contract
        run: node migration/phase-7/scripts/phase7a-contract.mjs

      - name: Run Phase 7B contract
        run: node migration/phase-7/scripts/phase7b-contract.mjs
