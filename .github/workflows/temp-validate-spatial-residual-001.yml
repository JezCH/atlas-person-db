name: TEMP Validate Spatial Residual 001

on:
  push:
    branches:
      - agent/spatial-reviewed-residual-001
    paths:
      - spatial/reviewed-bindings/shards/post-b51-residual-001.bindings.json
      - .github/workflows/temp-validate-spatial-residual-001.yml

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/spatial-reviewed-residual-001
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Validate reviewed shard against frozen baseline and taxonomy
        run: node scripts/compile-spatial-bindings.mjs --validate-only
