name: Phase 8B App Write Adapter

on:
  pull_request:
    paths:
      - "index.html"
      - "app.js"
      - "atlas-server-write-adapter.js"
      - "tests/phase8b-app-write-adapter.test.mjs"
      - "tests/atlas-server-write-adapter.test.mjs"
      - ".github/workflows/phase-8b-app-write-adapter.yml"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: atlas-phase-8b-app-write-adapter
  cancel-in-progress: false

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Use Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: Run app write routing tests
        run: node --test tests/phase8b-app-write-adapter.test.mjs

      - name: Run authenticated server adapter tests
        run: node --test tests/atlas-server-write-adapter.test.mjs

      - name: Emit evidence
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p migration/phase-8/tmp/phase8b-app-write-adapter
          printf '{"marker":"PHASE_8B_APP_WRITE_BOUNDARY","app_adapter_loaded":true,"app_direct_mutation":false,"mutation_boundary":"authenticated-server","client_fallback":false,"browser_mutation_secret":false}\n' > migration/phase-8/tmp/phase8b-app-write-adapter/verification.json

      - name: Upload evidence
        uses: actions/upload-artifact@v4
        with:
          name: phase-8b-app-write-adapter-${{ github.sha }}
          path: migration/phase-8/tmp/phase8b-app-write-adapter
          if-no-files-found: error
          retention-days: 90
