name: TEMP Validate Spatial Residual 002

on:
  push:
    branches:
      - agent/spatial-reviewed-residual-002
    paths:
      - spatial/reviewed-bindings/shards/post-b51-residual-002.bindings.json
      - .github/workflows/temp-validate-spatial-residual-002.yml

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/spatial-reviewed-residual-002
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Validate reviewed shard against frozen baseline and taxonomy
        run: node scripts/compile-spatial-bindings.mjs --validate-only
