name: Phase 8B Reconciliation Controller

on:
  pull_request:
    paths:
      - "atlas-reconciliation-controller.js"
      - "tests/atlas-reconciliation-controller.test.mjs"
      - "migration/phase-8/scripts/verify-phase8b-reconciliation-controller.mjs"
      - ".github/workflows/phase-8b-reconciliation-controller.yml"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: atlas-phase-8b-reconciliation-controller
  cancel-in-progress: false

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Use Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: Run controller tests
        shell: bash
        run: |
          set -euo pipefail
          node --test tests/atlas-reconciliation-controller.test.mjs

      - name: Verify controller remains unloaded and non-direct
        shell: bash
        run: |
          set -euo pipefail
          rm -rf migration/phase-8/tmp/phase8b-controller
          node migration/phase-8/scripts/verify-phase8b-reconciliation-controller.mjs \
            migration/phase-8/tmp/phase8b-controller/verification.json

      - name: Upload evidence
        uses: actions/upload-artifact@v4
        with:
          name: phase-8b-reconciliation-controller-${{ github.sha }}
          path: migration/phase-8/tmp/phase8b-controller
          if-no-files-found: error
          retention-days: 90
