import assert from 'node:assert/strict';import { rehearseP7SingleRewrite } from './lib/rehearse-stage2-p7-single-rewrite.mjs';await rehearseP7SingleRewrite({planPath:'stage2/execution/p7-solomon-chronology-execution.v1.json',sourceManifestPath:'stage2/authoring/p7-solomon-chronology-sources.v1.json',marker:'ATLAS_STAGE2_P7_SOLOMON_CHRONOLOGY_REHEARSAL_OK',verifyAfter:({actual,sourceLinks,baselineRow})=>{assert.equal(actual.person_id,baselineRow.person_id);assert.equal(actual.polity_id,baselineRow.polity_id);assert.equal(actual.relation_type_id,'7ca4de8f-01d4-542c-acc1-a06848c6742c');assert.equal(actual.activity_start,-970);assert.equal(actual.activity_end,-931);assert.equal(actual.activity_start_certainty,'approximate');assert.equal(actual.activity_end_certainty,'approximate');assert.equal(actual.chronology_status,'reviewed_stage2_traditional_disputed');assert.equal(actual.notes,baselineRow.notes);const ids=new Set(sourceLinks.map((r)=>r.source_id));assert.equal(ids.has('2dfe6d98-7e43-5d52-9b92-ce44f80dd58b'),true);assert.equal(ids.has('8d9ffd34-7f11-58d5-a3f2-0693ce853a8b'),true);}});
