# UI-7 scope boundary

This change is intentionally presentation-only. It does not change Person read semantics, Activity tuple semantics, filters, search, detail reads, duplicate logic, authoring mutation paths, Admin behavior, database schema, or Vercel deployment policy.
