# UI checkpoints

- UI-0 — branch isolation + information coverage baseline: COMPLETE
- UI-1 — read/API information inventory: NEXT
- UI-2 — Main Persons information-complete redesign
- UI-3 — Admin object inspector
- UI-4 — Admin System surface
- UI-5 — duplicate review UI alignment with settled P10 server lifecycle
- UI-6 — future object shells where backend authority exists
